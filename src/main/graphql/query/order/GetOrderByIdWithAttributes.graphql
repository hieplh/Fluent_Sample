query GetOrderByIdWithAttributes($id:ID!) {
    orderById(id:$id) {
        id
        ref
        status
        type
        attributes {
            name
            type
            value
        }
    }
}
