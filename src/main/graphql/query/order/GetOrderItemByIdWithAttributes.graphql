query GetOrderItemByIdWithAttributes($id:ID!) {
    orderItemById(id:$id) {
        id
        ref
        status
        attributes {
            name
            type
            value
        }
    }
}
