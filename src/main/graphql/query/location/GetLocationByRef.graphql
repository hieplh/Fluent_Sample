query getLocationByRef($locationRef: String) {
    location(ref: $locationRef) {
        id
        ref
        type
        status
        primaryAddress {
            latitude,
            longitude
        }
    }
}